# RemoteDesk 1.1.4 更新说明与版本号计划

## 目标

- 将应用当前版本从 `1.1.3 / 1001003` 提升到 `1.1.4 / 1001004`。
- 根据 1.1.3 以来已经提交的用户可感知变化，当前更新弹窗只展示 1.1.4 卡片；逐字保留原有 12 张 1.1.3 卡片作为历史版本数据，但不混入当前弹窗。
- 同步应用清单、NAPI、资源、诊断、README、用户指南、SBOM 与两套测试中的当前版本事实。

## 范围与约束

1. 新增 1.1.4 总览、敏感信息显示、剪贴板、应用分身、RustDesk 手机控制、键盘与会话界面、云恢复、RustDesk PC 翻转、五协议滚轮方向和最终欢迎页，共 10 张；当前弹窗总数为 10。
2. 原有 1.1.3 卡片的 `id`、图标、标题和描述不改动，只允许通过显式 `1.1.3` 版本查询读取；测试必须证明当前列表不含旧卡、历史列表仍为 12 张，且当前最终页为 `welcome-1-1-4`。
3. SBOM 的版本、源码锚点和创建时间必须一致且可追溯。
4. 不在本任务中安装 HAP、清除设备数据、推送远端或处理既有真机验收项。

## 验证

- `default@OhosTestCompileArkTS`
- 签名 `assembleHap`，并核对 `pack.info` 为 `1.1.4 / 1001004`
- `git diff --check`
- Light 开源合规门
- 独立只读复核；P0/P1/P2/P3 全部关闭后交付
