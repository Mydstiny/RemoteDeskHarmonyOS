# 1.0.6 Acceptance Baseline

Date: 2026-07-11

## Scope

- Baseline commit before the registered-native-test changes: `fcc652e6` (`fix(ui): use compatible mobile tab bar`).
- Worktree branch: `codex/1-0-6-functional-release`.

## ArkTS Test Target

Command:

```powershell
$env:DEVECO_SDK_HOME='C:\Program Files\Huawei\DevEco Studio\sdk'
$env:OHOS_SDK_HOME=$env:DEVECO_SDK_HOME
& 'C:\Program Files\Huawei\DevEco Studio\tools\node\node.exe' 'C:\Program Files\Huawei\DevEco Studio\tools\hvigor\bin\hvigorw.js' --mode module -p module=entry -p product=default default@OhosTestBuildArkTS --analyze=normal --parallel --incremental --daemon
```

Before the mobile tab-bar compatibility change, the first source diagnostic was `HostListPage.ets:2627:11 Declaration or statement expected`. The target now proceeds past `HostListPage` and no longer emits that parser diagnostic.

The target still exits nonzero because the DevEco SDK source-map stage throws `TypeError: Cannot read properties of undefined (reading 'share')` in `generate_sourcemap.js`. This is a toolchain follow-on failure, not an ArkTS source diagnostic from `HostListPage`.

## Native Registry Suite

Command:

```powershell
cmake -S entry\src\main\cpp -B build\rdp-native-tests -DRDP_BUILD_TESTS=ON
cmake --build build\rdp-native-tests --target rdp_native_tests --config Release
.\build\rdp-native-tests\rdp_native_tests.exe
```

The standalone registry tests are now built into `rdp_native_tests`. The registry cases `test_register_and_get`, `test_get_by_name`, `test_list_names`, `test_unregister`, and `test_extension_system_singleton` all pass. The macro-registered native suite reports `69 passed, 0 failed, 69 total`.

## Production Build Evidence

The production `assembleHap` command completed native compilation, ArkTS compilation, HAP packaging, and packing checks. It then failed at `SignHap` because the user-owned `build-profile.json5` references a missing local certificate:

```text
C:\Users\14288\Desktop\项目关键文档\证书\debug证书\RD_Client_Final_Debug.p12
```

No signing configuration was changed in this worktree.
