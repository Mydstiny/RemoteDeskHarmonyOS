# OHOS 交叉编译基础配置。
# 使用 scripts/macos_env.sh 或平台构建脚本注入当前 SDK 的 linker/sysroot。

[target.aarch64-unknown-linux-ohos]
linker = "clang++"
rustflags = [
    "-C", "link-arg=--target=aarch64-linux-ohos",
]

[target.x86_64-unknown-linux-ohos]
linker = "clang++"
rustflags = [
    "-C", "link-arg=--target=x86_64-linux-ohos",
]
